# 类型断言

```go
package main

import (
	"fmt"
)

func main() {
	a := "afads"
	typeAssert(a)
}

// 空接口可代指任何类型
func typeAssert(data interface{}) {
	// 类型断言
	switch data.(type) {
	case int:
		fmt.Println("Int")
	case string:
		fmt.Println("String")
	default:
		fmt.Println("Other Type")
	}
}
```
